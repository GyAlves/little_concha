// Modificação

/*linha de get_path
char	**get_path(void)//, char **envp)
{
	int		i;
	char 	**envar_path;

	//i = 0;
	/*envar = envp[i];
	while (envar != NULL)
	{
		printf("%s\n", envar);
		i++;
		envar = envp[i];
	}
	envar = getenv("PATH");
	if (envar == NULL)
	{
		printf("command not found: %s\n", v[1]);
		return (1);
	}
	printf("env variables: %s\n", envar);
	envar_path = getenv("PATH");
	if (envar_path == NULL)
		return ("/bin:/usr/bin");
	ft_split(envar_path, ':');
	return (envar_path);
}
*/